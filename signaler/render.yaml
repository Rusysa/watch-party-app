services:
  - type: web
    name: watchparty-signaler
    env: docker
    # Render evalúa esta ruta desde la raíz del repositorio.
    dockerfilePath: ./signaler/Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: "15325"
